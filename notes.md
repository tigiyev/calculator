## todo

- bug 3.2 + 0.1 = 3.3000000000000003

- bug = 1+1= (1)
- bug 1.0 + 2 = =
- bug 1.0 = (display preview shows 0 instead of 1)

- add decimals
	+ bug .2 has to be 0.2 
	- bug 0. have to be turned to 0 if clicked on operators
		// clear buffer 0.000 => 0
		// remove extra zeros 0.2000 => 0.2
	- bug 2.000 + (does not remove zeros from display first time )
	+ bug b cant have decimal
	+ add check if number has decimal every time, do not store true of false in variable 
- add backspace button
- add tousand separator
- add keyboard input



